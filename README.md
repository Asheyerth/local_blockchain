# local_blockchain  
Se debe crear un venv:   
  
apt install python3.12-venv #Instalar  
python3 -m venv env #Crear enviroment  
source env/bin/activate #Activarlo para Linux  
pip install -r requirements.txt #Instalar dependencias  